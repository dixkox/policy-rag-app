def answer_question(question: str, context: list[str]):
    # Minimal placeholder so the app runs
    # Replace with real LLM/RAG logic later
    joined = "\n".join(context)
    return f"Dummy answer for: {question}\nUsing context:\n{joined}"
